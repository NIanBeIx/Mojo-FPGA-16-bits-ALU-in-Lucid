module boolean (
    input alufn30[4],
    input a[16],
    input b[16],
    output out[16]
  ) {

  var i;
  always {
    for(i=0;i<16;i++){
    out[i]=alufn30[c{b[i],a[i]}];
    }
  }
}
